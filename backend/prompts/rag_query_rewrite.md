Generate {n} alternative phrasings of the following query to improve document retrieval.
Each alternative should capture the same intent from a different angle.
Return ONLY the alternatives, one per line, no numbering or explanation.

Original query: {query}
