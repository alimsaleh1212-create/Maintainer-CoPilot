# This file is a reference template only.
# The actual server block is generated at container startup by docker-entrypoint.sh
# based on the HOST_TYPE environment variable (allowed | disallowed).
#
# See frontend/demo_host/docker-entrypoint.sh for the runtime config generation.
